import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
  QrGenerator: {
    fontWeight:'bold',
    fontSize:40,
    marginTop: 20,
    textAlign: "center"
  }
});

export default styles;